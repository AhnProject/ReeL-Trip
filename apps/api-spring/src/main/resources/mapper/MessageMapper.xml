<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.reeltrip.api.chat.mapper.MessageMapper">

    <resultMap id="messageResultMap" type="com.reeltrip.api.chat.model.Message">
        <id     property="id"      column="id"/>
        <result property="spaceId" column="space_id"/>
        <result property="userId"  column="user_id"/>
        <result property="content" column="content"/>
        <result property="sentAt"  column="sent_at"/>
    </resultMap>

    <select id="findBySpaceId" resultMap="messageResultMap">
        SELECT id, space_id, user_id, content, sent_at
        FROM chat_messages
        WHERE space_id = #{spaceId}
        ORDER BY sent_at ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO chat_messages (space_id, user_id, content, sent_at)
        VALUES (#{spaceId}, #{userId}, #{content}, NOW())
    </insert>

</mapper>
