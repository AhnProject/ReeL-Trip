# Web app environment variables.
# Copy this file to `.env.local` and adjust as needed. `.env.local` is gitignored.

# Backend API base URL. Production backend is deployed to Railway.
# Server-side only (no NEXT_PUBLIC_ prefix) — used by Next.js rewrites as a proxy.
# Set this in Vercel/Railway env vars. DO NOT use NEXT_PUBLIC_API_URL in production.
API_URL=https://reel-tripapi-production.up.railway.app

# This web app's own URL (used for CORS / Server Actions allowlist).
NEXT_PUBLIC_APP_URL=http://localhost:5173
